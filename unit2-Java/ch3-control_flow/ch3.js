// Control Flow and Collections

// Control flow statements in Java are conditional and loops

// Java also uses switch statments. Remember that switch statments require the use of 'breaks' in most cases. Otherwise a behvaior known fallthrough can occur

// for-each loop
    // iterate over any sequence or collection
    // for (int i : nums)


// str.toCharArray() used to convert a string to an array

// The do-while loop is good for prompting for user input
    // it will run atleast once

// break and continue statments interupt the normal control flow
    // continue - terminates the only the 'current' iteration. 
        // for example - if looping through an array of 10 numbers, display "forth be with you" when it finds 4 but "not here" for everthing else
        /*
            for (int oneInt : someInts) {
            if (oneInt == 4) {
               System.out.println("forth be with you");
               continue;
            }
            System.out.println("Not here");
         }
        */

//* Data Structures
    // some examples are dictionaries, arrays, tuples, etc..
    // all values stored in Java collections must be objects

// Classes like ArrayList that take another type or class as a parameter are referred to as generic classes or generic types.