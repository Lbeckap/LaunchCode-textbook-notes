// Code With Carrie

// Use curly braces {} as a placeholder

// Using template literals in React

/*
    <Copyright author={`${user.firstname} ${user.lastname}`} />

*/

// <i> tag - used for icons. Remember to use a title for tool tips.

// Conditional Rendering - use if else statements or ternary operators.

// Components need to be Pascal Case = FlowerBed

// To render or to not render - use the && opertor

/*
    const BlogPost = props => {
        let { title, subtitle, paragraphs } = props;
        return (
            <>
            <Title text={title} />
            {subtitle && <Subtitle text={subtitle} />}
            <BlogContent text={paragraphs} />
            <Copyright author={user.fullName} />
            </>
        );
    };

*/

// Conditional Styling is a thing too

// .map() Method
    // React requires a unique key attribute

//? What is JSX and how to use it? 